package ch27.collectionframework.stack;

public class StackTheory {
    public static void main(String[] args) {
        /*
            Stack

            List 인테페이스를 구현한 컬렉션 클래스이다.
            Last In First Out이라 부르는 LIFO 자료구조를 가지고 있다.
            나중에 들어온 것이 가장 먼저 추출된다.

            리턴              메서드                     설명
            Object          push(Object o)              자료를 추가한다.
                                                        추가된 자료의 값을 반환한다.
            Object          pop()                       마지막 자료를 추출하고 해당 자료를 스택에서 삭제한다.
            Object          peek()                      스택에서 마지막 자료를 반환한다.
         */
    }
}
